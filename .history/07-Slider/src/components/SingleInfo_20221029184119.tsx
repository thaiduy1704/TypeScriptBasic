import React from 'react';

export interface IPerson {
	id: number;
	image: string;
	name: string;
	title: string;
	quote: string;
}
const SingleInfo = () => {
	return <div>SingleInfo</div>;
};

export default SingleInfo;
