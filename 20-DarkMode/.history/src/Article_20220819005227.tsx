import moment from 'moment';
import styled from 'styled-components';
import { IArticle } from './typing';

const Article = ({title,date,length}) => {
	return <div>Article</div>;
};

export default Article;
