import moment from 'moment';
import styled from 'styled-components';
import { IArticle } from './typing';

const Article = ({title,date,length,snip}) => {
	return <div>Article</div>;
};

export default Article;
